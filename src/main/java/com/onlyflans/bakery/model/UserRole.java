package com.onlyflans.bakery.model;

// Clase enum para definir los roles de usuario
public enum UserRole {
    NORMAL, // Usuario estándar de la tienda.
    ADMIN   // Usuario con permisos de admin.
}
